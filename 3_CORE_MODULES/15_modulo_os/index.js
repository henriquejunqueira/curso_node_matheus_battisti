const os = require('os');

console.log(os.cpus()); // número de cpus
console.log(os.freemem()); // quantidade de memória ram livre
console.log(os.homedir()); // diretório home
console.log(os.type()); // tipo de SO
